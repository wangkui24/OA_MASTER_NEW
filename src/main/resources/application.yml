spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  datasource:
    url: jdbc:mysql://10.0.16.12:3306/employee?useUnicode=true&characterEncoding=UTF-8&useSSL=false&useTimezone=true&serverTimezone=GMT%2B8
    username: root
    password: wangkui123
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
  database: mysql
  hibernate:
    ddl-auto: update #自动更新
  show-sql: true  #开发阶段,打印要执行的sql语句.

logging:
  level:
    root: debug

server:
  port: 8888