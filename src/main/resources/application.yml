spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: wangkui123
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
  database: mysql
  hibernate:
    ddl-auto: update #自动更新
  show-sql: true  #开发阶段,打印要执行的sql语句.

logging:
  level:
    root: debug

server:
  port: 8888